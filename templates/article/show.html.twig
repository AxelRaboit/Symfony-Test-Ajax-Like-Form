{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ article.title }}</h1>
        <p>{{ article.description }}</p>
    </div>

    <a href="{{ path('home') }}">back</a>
{% endblock %}
